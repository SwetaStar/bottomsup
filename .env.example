# ONE key. Server-side only. Never prefix with NEXT_PUBLIC_.
#
# Source of truth is the Vercel project's Environment Variables.
# A local .env.local is optional — only needed if you run `npm run dev` or the
# optional `npm run embed` script on your own machine.
#
# Get a free key (no card): https://aistudio.google.com/apikey

GEMINI_API_KEY=
